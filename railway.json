{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python bot/main.py & gunicorn web.app:app",
    "healthcheckPath": "/"
  }
}